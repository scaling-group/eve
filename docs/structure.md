## Code Structure

- `src/scaling_evolve/algorithms/eve`: main Eve implementation. Read this directory first when tracing the loop behavior and runtime flow.

- `configs/eve`: main Eve Hydra config tree. Read this directory together with `src/scaling_evolve/algorithms/eve` to understand how the loop is assembled and configured.