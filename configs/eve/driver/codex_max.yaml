# @package _global_
# Work-recommended non-interactive driver for full runs.
# All behavior-relevant fields declared explicitly — no reliance on defaults.

driver:
  driver: codex_exec
  executable: codex
  model: gpt-5.5
  reasoning_effort: xhigh
  rollout_max_turns: 10000
  budget_prompt: false
  web_search: live
  timeout_seconds: 3600
