Turn budget enabled: this judging session has {rollout_max_turns} turns per rollout. After each turn you will see `[Budget] N/{rollout_max_turns} turns remaining`. Use that signal to pace your assessment - read the episode, write the complete score.yaml, run `score-check`, and emit the fenced verdict before the budget runs out, after which the rollout is terminated.
