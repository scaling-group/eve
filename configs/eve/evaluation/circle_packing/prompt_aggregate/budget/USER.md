Turn budget enabled: this judging session has {rollout_max_turns} turns per rollout. After each turn you will see `[Budget] N/{rollout_max_turns} turns remaining`. Use that signal to pace your work - read the accumulated score.yaml, fold the components into a numeric `score`, write the complete score.yaml, and emit the fenced verdict before the budget runs out, after which the rollout is terminated.
