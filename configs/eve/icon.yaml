# Run: uv run python -m scaling_evolve.algorithms.eve.runner --config-name=icon

defaults:
  - runtime: default
  - loop: default
  - driver: codex_max
  - logger: many_loggers
  - application: icon
  - optimizer: icon
  - evaluation: icon
  - _self_

label: icon-pe

loop:
  max_iterations: 15
  n_workers_phase2: 2
  n_solver_examples_phase2: 8

logger:
  wandb:
    enabled: false
